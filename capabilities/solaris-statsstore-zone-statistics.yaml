naftiko: 1.0.0-alpha2
info:
  label: Solaris Zones Oracle Solaris StatsStore and Analytics API — Zone Statistics
  description: 'Solaris Zones Oracle Solaris StatsStore and Analytics API — Zone Statistics. 1 operations. Lead operation:
    Solaris Zones Read Zone-specific Statistics. Self-contained Naftiko capability covering one Solaris Zones business surface.'
  tags:
  - Solaris Zones
  - Zone Statistics
  created: '2026-05-19'
  modified: '2026-05-19'
binds:
- namespace: env
  keys:
    SOLARIS_ZONES_API_KEY: SOLARIS_ZONES_API_KEY
capability:
  consumes:
  - type: http
    namespace: solaris-statsstore-zone-statistics
    baseUri: https://{host}:{port}/api/com.oracle.solaris.rad.sstore/1.0
    description: Solaris Zones Oracle Solaris StatsStore and Analytics API — Zone Statistics business capability. Self-contained,
      no shared references.
    resources:
    - name: Store-_rad_method-readZoneStats
      path: /Store/_rad_method/readZoneStats
      operations:
      - name: readzonestatistics
        method: PUT
        description: Solaris Zones Read Zone-specific Statistics
        outputRawFormat: json
        outputParameters:
        - name: result
          type: object
          value: $.
        inputParameters:
        - name: body
          in: body
          type: object
          description: Request body (JSON).
          required: true
    authentication:
      type: apikey
      key: _rad_session
      value: '{{env.SOLARIS_ZONES_API_KEY}}'
      placement: cookie
  exposes:
  - type: rest
    namespace: solaris-statsstore-zone-statistics-rest
    port: 8080
    description: REST adapter for Solaris Zones Oracle Solaris StatsStore and Analytics API — Zone Statistics. One Spectral-compliant
      resource per consumed operation, prefixed with /v1.
    resources:
    - path: /v1/store/rad-method/readzonestats
      name: store-rad-method-readzonestats
      description: REST surface for Store-_rad_method-readZoneStats.
      operations:
      - method: PUT
        name: readzonestatistics
        description: Solaris Zones Read Zone-specific Statistics
        call: solaris-statsstore-zone-statistics.readzonestatistics
        with:
          body: rest.body
        outputParameters:
        - type: object
          mapping: $.
  - type: mcp
    namespace: solaris-statsstore-zone-statistics-mcp
    port: 9090
    transport: http
    description: MCP adapter for Solaris Zones Oracle Solaris StatsStore and Analytics API — Zone Statistics. One tool per
      consumed operation, routed inline through this capability's consumes block.
    tools:
    - name: solaris-zones-read-zone-specific
      description: Solaris Zones Read Zone-specific Statistics
      hints:
        readOnly: false
        destructive: false
        idempotent: true
      call: solaris-statsstore-zone-statistics.readzonestatistics
      with:
        body: tools.body
      outputParameters:
      - type: object
        mapping: $.
